<Window x:Class="DemoSocialNetwork.Views.LogInView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoSocialNetwork.Views"
        mc:Ignorable="d"
        Title="LogInView" Height="250" Width="250"
        DataContext="{StaticResource vmlocator}"
        x:Name="wnd">
    <Grid DataContext="{Binding LogInViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Margin="20">
            <TextBlock Text="Login:"></TextBlock>
            <TextBox Text="{Binding User.UserAuthorizationInfo.Login}"></TextBox>
            <TextBlock Text="Password:"></TextBlock>
            <TextBox Text="{Binding User.UserAuthorizationInfo.Password}"></TextBox>
            <Button Command="{Binding LogInCommand}">Log In</Button>
            <TextBlock Text="{Binding ErrorMessage}"></TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Dont have an account?"></TextBlock>
            <Button Command="{Binding RegistrationCommand}" CommandParameter="{Binding ElementName=wnd}">Create</Button>
        </StackPanel>
    </Grid>
</Window>
